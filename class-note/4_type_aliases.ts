interface IPerson{
    name : string;
    age : number;
}

type TPerson = {
    name : string;
    age:number;
}

var siho : IPerson ={
    name : "시호",
    age :  30,
};

var seko : TPerson = {
    name : 'se',
    age : 56,
}

// type Mystring = string;